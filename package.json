{
  "name": "pi-python",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "pi extension that exposes a persistent Python execution environment to the agent",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-transform-types --no-warnings test-restore.ts"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.70.2",
    "@mariozechner/pi-tui": "^0.70.2",
    "@types/node": "^22.0.0",
    "typebox": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
